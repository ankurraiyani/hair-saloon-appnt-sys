import { ShopkeeperDurationPipe } from './shopkeeper-duration.pipe';

describe('ShopkeeperDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new ShopkeeperDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
